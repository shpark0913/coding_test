## 순차 탐색 (Sequential Seartch)
- 리스트 안에 있는 특정한 데이터를 찾기 위해 앞에서부터 데이터를 하나씩 차례대로 확인하는 방법
- count() 메서드 내부에서도 순차 탐색이 수행됨

## 이진 탐색 : 반으로 쪼개면서 탐색하기
- 이진 탐색(Binary Search)은 배열 내부의 데이터가 정렬되어 있어야만 사용할 수 있는 알고리즘
- 데이터가 무작위일 떄는 사용할 수 없지만, 이미 정렬되어 있따면 매우 빠르게 데이터를 찾을 수 있음
- 변수 3개 사용
  - 시작점, 끝점, 중간점
  - 찾으려는 데이터와 중간점 위치에 있는 데이터를 반복적으로 비교해서 원하는 데이터를 찾음
  - 시간 복잡도 : O(logN)